\model{A C++ Program}
  \begin{center}
    \begin{minipage}{4in}
      \begin{cpplst}
string str = "Global Scope";

void myFunc() {
  cout << str << endl;
  string str = "myFunc Scope";
  cout << str << endl;
}

int main() {
  string str = "Main Scope";
  if (true) {
    cout << str << endl;
    string str = "If Scope";
    myFunc();
    cout << str << endl;
  }
  cout << str << endl;
}                       
      \end{cpplst}
    \end{minipage}
    \begin{minipage}{2in}
      {\bf Output:}
      \hrule
      \begin{cpplst}
Main Scope
Global Scope
myFunc Scope
If Scope
Main Scope
      \end{cpplst}
    \end{minipage}
  \end{center}
                                                
  {\it\large Refer to Model 3 above as your team develops consensus answers
    to the questions below.}        

  \Q In the program above, the {\tt str} identifier is used for several different
    variables.  Determine the line number of the \cpp{cout << str << endl;}
    command that produces each line of output. The code can be found in {\tt activity14c.cpp}. 
    \begin{enumerate}
      \itemsep 10pt
      \begin{multicols}{2}
        \item The first {\tt Main Scope}  \hspace{0.25in} \ans[1in]{Line 12}
        \item {\tt Global Scope}          \hspace{0.25in} \ans[1in]{Line 4}
        \item {\tt myFunc Scope}          \hspace{0.25in} \ans[1in]{Line 6}
        \item {\tt If Scope}              \hfill \ans[1in]{Line 15}
        \item The second {\tt Main Scope} \hfill \ans[1in]{Line 17}
      \end{multicols}
    \end{enumerate}
    
  \Q When a variable declared within one scope blocks access to a variable of the same
    name declared in an outer scope (that contains the first), we say that the outer variable
    has been {\it shadowed} or that the name of the variable has been {\it masked}.  
    Give two instances of variable shadowing in the code above.
    \begin{answer}[1in]
      \begin{itemize}
        \item The global variable on line 1 is shadowed by the variable defined on line 5
          within the function.
        \item The local variable on line 10 is shadowed by the variable defined on line 13
          in the \cpp{if} block.
      \end{itemize}
    \end{answer}

  \Q Say we changed the function header on line 3 to \cpp{void myFunc(string str)} 
    and\key\\[-2.5mm] the function call on line 14 to \cpp{myFunc("Parameter Scope")}.
    How would the output change? 
    \begin{answer}[0.5in]
      The ``Global Scope'' line would now say ``Parameter Scope''.
    \end{answer}